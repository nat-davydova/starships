import React from "react";

import Table from "components/Table/Table";

const comparisonTable = ({ config, theadProps, tbodyProps }) => (
  <Table config={config} tbodyProps={tbodyProps} />
);

export default comparisonTable;
